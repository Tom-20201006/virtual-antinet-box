# TASKS

## 已完成

- 创建 Godot 主场景 `res://scenes/Main.tscn`。
- 创建脚本结构：卡片、分隔片、抽屉、卡片盒、摄像机、交互、导入、世界状态、物理睡眠管理。
- 创建 3D 桌面和三个功能区域。
- 创建卡片盒与 3 个可开合抽屉。
- 创建默认测试卡片与分隔片。
- 实现选择、拖拽、旋转、翻面、放置、插入抽屉、从抽屉取出。
- 实现本地图片导入为卡片正面贴图。
- 实现 JSON 状态保存与加载。
- 创建项目文档。

## 下一步建议

- 增加可选背面图片导入流程。
- 增加更清楚的抽屉内多卡片分页或层级显示。
- 引入真正的对象池管理器，避免大量卡片频繁创建和释放。
- 增加缩略图贴图缓存。
- 增加可选的截图式验证脚本。

## 不做

- OCR、搜索、标签、自动分类、自动链接、AI 摘要、知识图谱、文本理解。
